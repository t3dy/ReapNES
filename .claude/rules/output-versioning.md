---
paths:
  - "output/**"
  - "studio/reaper_projects/**"
---

# Output Versioning

- NEVER overwrite a MIDI, WAV, RPP, or MP4 file the user has tested.
- Always use version suffixes: `_v1.mid`, `_v2.rpp`, `_v3.wav`.
- When generating a new version, announce the filename before creating it.
